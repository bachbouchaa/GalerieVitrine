{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-3">
        <i class="bi bi-save me-2"></i>{{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}
